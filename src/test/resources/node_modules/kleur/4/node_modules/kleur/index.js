const passthrough = (value = '') => String(value);

function chain() {
  return new Proxy(passthrough, {
    get() {
      return chain();
    },
  });
}

export default new Proxy({}, {
  get() {
    return chain();
  },
});
